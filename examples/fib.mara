(def + (import "core" "+"))
(def - (import "core" "-"))
(def < (import "core" "<"))

(def fib)
(set fib (fn (n)
           (if (< n 1)
             n
             (+ (fib (- n 1))
                (fib (- n 2))))))

(fib 4)
